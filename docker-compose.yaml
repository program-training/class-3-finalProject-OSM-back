version: '3'

services:
  test:
    image: postgres
    environment:
      POSTGRES_PASSWORD: 12345
    ports:
      - "5432:5432"

  app:
    build: .
    command: npm start
    ports:
      - "8081:8080"
    depends_on:
      - test
